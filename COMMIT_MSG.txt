fix: pass transcript context in /ask fallback path

When the intent detector returns None (e.g. "summarize" with no
matching kind), the fallback search now includes transcript context
so Ollama can actually answer about the meeting.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
