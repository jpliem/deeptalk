fix: route POST /ask through intent detector instead of hardcoding search

The /ask endpoint now detects intent and fires the appropriate agent
(search/proscons/planning/mockup) instead of always running search.
Stores fire callback and detector on app.state so they're accessible
from request handlers.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
