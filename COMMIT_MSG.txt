feat: inject transcript context into search agent

Agents now receive the last 20 lines of transcript so they can answer
questions about the meeting. The search agent prepends context before
sending the query to the LLM. Also added **kwargs to all agent runners
to allow extensible keyword passing from dispatch.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
