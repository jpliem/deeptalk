fix: skip intent detection on manual chat (/ask)

The textbox is a manual action — just answer the question directly via
search agent with transcript context. No intent routing.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
